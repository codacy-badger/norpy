# We want to work in a dedicated environment
source activate norpy

# We need access to our package
export PYTHONPATH=$PWD:$PYTHONPATH

# We need access to the ose_utils submodule
export PYTHONPATH=$PWD/submodules/ose_utils:$PYTHONPATH
